# Agent tool staging zone

The maintenance Agent may author tool source or manifests here. Tool files are
staged for owner review and are never auto-executed by the bot. This prevents a
new tool from bypassing the host's privacy, destination, and filesystem guards.
